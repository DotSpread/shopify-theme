
<div class="collection-list">

{% for collection in collections %}

    {% capture collection_title %}{{ collection.title | escape }}{% endcapture %}

    <div class="collection-list-item">

      {% if collection.image %}

        <img class="collection-list-image" src="{{ collection.image | img_url: 'medium' }}" alt="{{ collection_title }}" />

      {% endif %}

        <h2 class="collection-list-title"><a href="{{ collection.url }}">More {{ collection_title }} &rsaquo;</a></h2>

      {% for product in collection.products limit:5 %}

        {% assign grid_item_width = 'large--one-fifth medium--one-half' %}

        <div>

          <a href="{{ product.url | within: collection }}">{{ product.title }}</a>

          {{ product.price | money }}

          {% unless product.available %}<br><strong>sold out</strong>{% endunless %}

          <a href="{{ product.url | within: collection }}">
            <img src="{{ product.featured_image.src | img_url: 'large' }}" alt="{{ product.featured_image.alt | escape }}">
          </a>

        </div>

      {% endfor %}

    </div><!-- collection-list-item -->

  {% endfor %}

</div><!-- / .collection-list -->
